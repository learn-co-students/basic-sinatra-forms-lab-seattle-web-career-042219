<!DOCTYPE html>
<html>
 <head>
  <meta charset="UTF-8">
  <title> Team Name:  @team_name</title>
 </head>
 <body>

<% @params.each do |key, val|%>
<% key2 = key.to_s.split('_').join(' ').capitalize%>
<% stat = "#{key2}: #{val}"%>
  <h2><%= stat%></h2>
<% end %>
 </body>
</html>
